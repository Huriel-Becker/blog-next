/** @type {import('next').NextConfig} */
/* eslint-disable no-undef */
/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true, distDir: 'out_publish' };
module.exports = nextConfig;
